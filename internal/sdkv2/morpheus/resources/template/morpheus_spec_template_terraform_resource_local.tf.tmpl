resource "hpe_morpheus_spec_template_terraform" "tfexample_terraform_spec_terraform_local" {
  name         = "{{.Name}}"
  source_type  = "{{.SourceType}}"
  spec_content = <<EOF
{{.SpecContent}}
EOF
}
