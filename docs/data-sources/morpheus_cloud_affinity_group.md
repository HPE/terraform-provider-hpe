---
page_title: "hpe_morpheus_cloud_affinity_group Data Source - terraform-provider-hpe"
subcategory: "Morpheus"
description: |-
  
---
# hpe_morpheus_cloud_affinity_group (Data Source)



~> **Terminology** Morpheus models these as separate things. An **instance** is the
unit you provision and manage. A **compute server** is a machine record backing it, and
may be either a guest virtual machine or a hypervisor host — both appear in the same
collection. A **host** is a compute server that hosts others: it has guests of its own
and no parent. Attributes named `compute_server_id`, `compute_servers` or `servers`
therefore refer to machine records, which in practice are usually guests; `parent_host_id`
names the hypervisor a guest runs on.

-> Unlike the [`hpe_morpheus_cloud_affinity_group`](https://registry.terraform.io/providers/HPE/hpe/latest/docs/resources/morpheus_cloud_affinity_group) resource, this data source also returns affinity groups that Morpheus imported from vCenter DRS rules on a VMware cloud. Check `source` to tell a group created by a user (`"user"`) from an imported one (`"sync"`). This data source is the supported way to reference an imported group.

-> Note that Morpheus version `8.0.10` or later is required for affinity group support.

## Example Usage - Name

```terraform
data "hpe_morpheus_cloud_affinity_group" "example" {
  name     = "default"
  cloud_id = 1
}
```

## Example Usage - Id

```terraform
data "hpe_morpheus_cloud_affinity_group" "example" {
  id       = 1
  cloud_id = 1
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `cloud_id` (Number) The ID of the parent cloud.

### Optional

- `id` (Number) The ID of the cloud affinity group.
- `name` (String) The name of the cloud affinity group.

### Read-Only

- `active` (Boolean) Whether the affinity group is active.
- `affinity_type` (String) The affinity type (KEEP_TOGETHER or KEEP_SEPARATE).
- `pool` (Attributes) The resource pool associated with the affinity group. (see [below for nested schema](#nestedatt--pool))
- `ref_id` (Number) The reference ID.
- `ref_type` (String) The reference type.
- `resource_permissions` (Attributes) Resource permissions for group access. (see [below for nested schema](#nestedatt--resource_permissions))
- `servers` (Set of Number) Set of compute server IDs in the affinity group.
- `source` (String) The source of the affinity group (e.g. user, sync).
- `tenant_ids` (Set of Number) Tenant account IDs allowed access. The Morpheus API does not currently apply tenant assignment on affinity groups, so this is typically empty regardless of what has been requested. Tracked as MORPH-15806.
- `visibility` (String) The visibility of the affinity group.

<a id="nestedatt--pool"></a>
### Nested Schema for `pool`

Read-Only:

- `id` (Number) The ID of the resource pool.


<a id="nestedatt--resource_permissions"></a>
### Nested Schema for `resource_permissions`

Read-Only:

- `all` (Boolean) Whether access is allowed to all groups.
- `groups` (Attributes Set) Set of groups allowed access. (see [below for nested schema](#nestedatt--resource_permissions--groups))

<a id="nestedatt--resource_permissions--groups"></a>
### Nested Schema for `resource_permissions.groups`

Read-Only:

- `default` (Boolean) Whether this is the default group.
- `id` (Number) Group ID.
