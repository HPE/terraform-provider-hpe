---
page_title: "hpe_morpheus_cluster_affinity_groups Data Source - terraform-provider-hpe"
subcategory: "Morpheus"
description: |-
  
---
# hpe_morpheus_cluster_affinity_groups (Data Source)



~> **Terminology** Morpheus models these as separate things. An **instance** is the
unit you provision and manage. A **compute server** is a machine record backing it, and
may be either a guest virtual machine or a hypervisor host — both appear in the same
collection. A **host** is a compute server that hosts others: it has guests of its own
and no parent. Attributes named `compute_server_id`, `compute_servers` or `servers`
therefore refer to machine records, which in practice are usually guests; `parent_host_id`
names the hypervisor a guest runs on.

-> Cluster affinity groups are always created by a user, so every entry's `source` is `"user"` here. Affinity groups imported from vCenter DRS rules are cloud-scoped rather than cluster-scoped, and are returned by [`hpe_morpheus_cloud_affinity_groups`](https://registry.terraform.io/providers/HPE/hpe/latest/docs/data-sources/morpheus_cloud_affinity_groups) instead.

-> Note that Morpheus version `8.0.10` or later is required for affinity group support.

## Example Usage

```terraform
data "hpe_morpheus_cluster_affinity_groups" "example" {
  cluster_id = 1

  filter {
    name   = "affinity_type"
    values = ["KEEP_TOGETHER"]
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `cluster_id` (Number) The ID of the parent cluster.

### Optional

- `filter` (Block Set) Filter block. Repeat to apply multiple filters (all ANDed together). Filter values are case-sensitive and support Go regular expressions (https://regex101.com/). (see [below for nested schema](#nestedblock--filter))

### Read-Only

- `affinity_groups` (Attributes Set) The list of affinity groups matching the supplied filters. (see [below for nested schema](#nestedatt--affinity_groups))

<a id="nestedblock--filter"></a>
### Nested Schema for `filter`

Required:

- `name` (String) The field to filter on. Valid names are: name, affinity_type, source, visibility.
- `values` (Set of String) The filter values. An affinity group matches the block if the chosen field matches ANY value (Go regular expression).


<a id="nestedatt--affinity_groups"></a>
### Nested Schema for `affinity_groups`

Read-Only:

- `active` (Boolean) Whether the affinity group is active.
- `affinity_type` (String) The affinity type (KEEP_TOGETHER or KEEP_SEPARATE).
- `id` (Number) The ID of the affinity group.
- `name` (String) The name of the affinity group.
- `pool` (Attributes) The resource pool associated with the affinity group. (see [below for nested schema](#nestedatt--affinity_groups--pool))
- `ref_id` (Number) The reference ID.
- `ref_type` (String) The reference type.
- `resource_permissions` (Attributes) Resource permissions for group access. (see [below for nested schema](#nestedatt--affinity_groups--resource_permissions))
- `servers` (Set of Number) Set of compute server IDs in the affinity group.
- `source` (String) The source of the affinity group (e.g. user, sync).
- `tenant_ids` (Set of Number) Tenant account IDs allowed access. The Morpheus API does not currently apply tenant assignment on affinity groups, so this is typically empty regardless of what has been requested. Tracked as MORPH-15806.
- `visibility` (String) The visibility of the affinity group.

<a id="nestedatt--affinity_groups--pool"></a>
### Nested Schema for `affinity_groups.pool`

Read-Only:

- `id` (Number) The ID of the resource pool.


<a id="nestedatt--affinity_groups--resource_permissions"></a>
### Nested Schema for `affinity_groups.resource_permissions`

Read-Only:

- `all` (Boolean) Whether access is allowed to all groups.
- `groups` (Attributes Set) Set of groups allowed access. (see [below for nested schema](#nestedatt--affinity_groups--resource_permissions--groups))

<a id="nestedatt--affinity_groups--resource_permissions--groups"></a>
### Nested Schema for `affinity_groups.resource_permissions.groups`

Read-Only:

- `default` (Boolean) Whether this is the default group.
- `id` (Number) Group ID.
