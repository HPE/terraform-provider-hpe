---
page_title: "hpe_morpheus_network_transport_zone Data Source - terraform-provider-hpe"
subcategory: "morpheus"
description: |-
  Reads a Morpheus network transport zone (NSX-T).
---
# hpe_morpheus_network_transport_zone (Data Source)

Reads a Morpheus network transport zone (NSX-T). This data source can be used to look up an existing transport zone by ID or name to reference its attributes in other resources.

The `config` attribute contains the full configuration object returned by the API (e.g. `nvdsName`, `hostMembershipCriteria`).

## Example Usage

### Look up by ID

```terraform
data "hpe_morpheus_network_transport_zone" "example" {
  network_server_id = 1
  id                = 99
}
```

### Look up by Name

```terraform
data "hpe_morpheus_network_transport_zone" "example" {
  network_server_id = 1
  name              = "overlay-tz-01"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `network_server_id` (Number) The ID of the network server (integration) this transport zone belongs to

### Optional

- `id` (Number) The ID of the network transport zone
- `name` (String) The name of the network transport zone

### Read-Only

- `active` (Boolean) Whether the transport zone is active
- `config` (Dynamic) Transport zone configuration object
- `display_name` (String) The display name of the transport zone
- `enabled` (Boolean) Whether the transport zone is enabled
- `external_id` (String) The external identifier of the transport zone in the network provider
- `internal_id` (String) The internal identifier of the transport zone
- `provider_id` (String) The provider-assigned identifier for the transport zone
- `status` (String) The current status of the transport zone
- `stream_type` (String) The stream type of the transport zone (e.g. OVERLAY, VLAN)
- `visibility` (String) The visibility of the transport zone (e.g. public, private)
