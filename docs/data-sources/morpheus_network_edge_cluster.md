---
page_title: "hpe_morpheus_network_edge_cluster Data Source - terraform-provider-hpe"
subcategory: "morpheus"
description: |-
  Reads a Morpheus network edge cluster (NSX-T).
---
# hpe_morpheus_network_edge_cluster (Data Source)

Reads a Morpheus network edge cluster (NSX-T). This data source can be used to look up an existing edge cluster by ID or name to reference its attributes in other resources.

The `config` attribute contains the full configuration object returned by the API (e.g. `clusterProfileBindings`, `members`, `deploymentType`, `memberNodeType`).

## Example Usage

### Look up by ID

```terraform
data "hpe_morpheus_network_edge_cluster" "example" {
  network_server_id = 1
  id                = 99
}
```

### Look up by Name

```terraform
data "hpe_morpheus_network_edge_cluster" "example" {
  network_server_id = 1
  name              = "edge-cluster-01"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `network_server_id` (Number) The ID of the network server (integration) this edge cluster belongs to

### Optional

- `id` (Number) The ID of the network edge cluster
- `name` (String) The name of the network edge cluster

### Read-Only

- `active` (Boolean) Whether the edge cluster is active
- `config` (Dynamic) Edge cluster configuration object
- `display_name` (String) The display name of the edge cluster
- `enabled` (Boolean) Whether the edge cluster is enabled
- `external_id` (String) The external identifier of the edge cluster in the network provider
- `internal_id` (String) The internal identifier of the edge cluster
- `provider_id` (String) The provider-assigned identifier for the edge cluster
- `visibility` (String) The visibility of the edge cluster (e.g. public, private)
