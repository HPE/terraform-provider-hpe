# token
Handles the GLCS/GLP IAM OAuth2.0 Token exchange.

OAuth2 credentials (client_id, client_secret) are exchanged for a GLCS/GLP IAM token.

Adapted from `github.com/hewlettpackard/hpegl-provider-lib`.
