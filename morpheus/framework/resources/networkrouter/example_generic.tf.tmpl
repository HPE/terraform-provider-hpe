resource "hpe_morpheus_network_router" "example" {
  name                   = "{{.Name}}"
  type_id                = {{.TypeId}}
  group_id               = {{.GroupId}}
  network_integration_id = {{.NetworkIntegrationId}}
  enabled                = true

  config = {
  }
}
