resource "hpe_morpheus_network_pool" "example" {
  name           = "{{.Name}}"
  type_id        = {{.TypeId}}
  subnet_address = "{{.SubnetAddress}}"
  netmask        = "{{.Netmask}}"
  gateway        = "{{.Gateway}}"
  dns_domain     = "{{.DnsDomain}}"
}
