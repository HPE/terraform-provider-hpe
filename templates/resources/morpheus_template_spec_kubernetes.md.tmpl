---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: {{ $arr := split .Name "_" }}"{{ index $arr 1 }}"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---
# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Example Usage

Creating the kubernetes spec template with local content:

{{tffile "examples/resources/morpheus_template_spec_kubernetes/resource_local.tf"}}

Creating the kubernetes spec template with the template fetched from a url:

{{tffile "examples/resources/morpheus_template_spec_kubernetes/resource_url.tf"}}

Creating the kubernetes spec template with the template fetched via git:

{{tffile "examples/resources/morpheus_template_spec_kubernetes/resource_git.tf"}}

{{ .SchemaMarkdown | trimspace }}

## Import

Import is supported using the following syntax:

{{codefile "shell" "examples/resources/morpheus_template_spec_kubernetes/import.sh" }}
