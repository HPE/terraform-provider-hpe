---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: {{ $arr := split .Name "_" }}"{{ index $arr 1 }}"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---
# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Example Usage

Creating the terraform spec template with local content:

{{tffile "examples/resources/morpheus_template_spec_terraform/resource_local.tf"}}

Creating the terraform spec template with the template fetched from a url:

{{tffile "examples/resources/morpheus_template_spec_terraform/resource_url.tf"}}

Creating the terraform spec template with the template fetched via git:

{{tffile "examples/resources/morpheus_template_spec_terraform/resource_git.tf"}}

{{ .SchemaMarkdown | trimspace }}

## Import

Import is supported using the following syntax:

{{codefile "shell" "examples/resources/morpheus_template_spec_terraform/import.sh" }}
